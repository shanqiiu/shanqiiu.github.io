# 2026-08-18 工作日志

## 接入诗泉诗词 API（每日随机诗词）
- 端点 `GET https://poetry.palemoky.com/api/poems/random`，CORS `Access-Control-Allow-Origin: *`（浏览器 fetch 直连，无需服务端代理）。返回 `data.{title, content:[逐句数组], author:{name}, dynasty:{name}, type:{name}}`；支持 `?author= / ?dynasty= / ?type= / ?char=` 过滤。
- 改动 `assets/js/main.js` 的 `initHeroQuote()`：加载与点击均优先 `fetch` 该 API 取随机诗词；多行 `content` 逐行 `escapeHtml` 后 `<br>` 拼接（防 XSS）；来源格式 `朝代·作者《标题》`；`fetch` 失败/网络异常时回退本地 `data/quotes.yaml` 随机名句（graceful degradation）。
- 验证：jsdom 模拟 `fetch`——API 正常→渲染诗词（多行+署名），`FAIL=1`→回退本地名句，均通过。
- 坑：用 Git-Bash 风格绝对路径（`/tmp`、`/c/Users...`）作 `hugo --destination` 时，hugo 会把路径当作相对路径在仓库根误建 `c/`、`tmp/` 整站副本（各 19M）。已同盘改名移出仓库（`D:/_hugo_stray_builds`）。教训：hugo 构建验证请用仓库内相对目录（如 `.vb_verify`，已在 .gitignore）。

## 修复「每日一句」循环按钮点击无反应
现象：首页右上角诗句下方的圆形刷新按钮单击无反应，且诗句始终停在静态兜底句。

根因（两处）：
1. **数据二次编码**：`layouts/index.html` 用 `{{ hugo.Data.quotes.items | jsonify }}` 注入名句库，但本机 Hugo v0.164.0 对 `hugo.Data` 下的 slice 做 `jsonify` 会输出成**带引号的 JSON 字符串**（即 `"[{...}]"`），`JSON.parse` 得到 string 而非 array，`initHeroQuote` 在 `Array.isArray` 守卫处直接 return → 静态兜底、按钮无事件。
   - 修复：改为裸输出 `{{ hugo.Data.quotes.items }}`（不加 jsonify），产物即为合法 JSON 数组。
2. **boot() 无容错**：`initVisitorStats` 直接 `fetch(...)` 未守卫，一旦抛错会中断整个 `boot()`，导致排在最后的 `initHeroQuote` 不执行。
   - 修复：`boot()` 内所有 init 改为 `safe(label, fn)` 包裹，单点失败不再连累后续。

改动文件：`layouts/index.html`（hero-quotes-data 去 jsonify）、`assets/js/main.js`（initHeroQuote 解析容错 + boot 加 safe 包裹）、`data/quotes.yaml`（名句整体包到 `items:` 键下，便于 hugo.Data.quotes.items 取出）。

验证：hugo --noBuildLock 构建通过；用 jsdom 加载产物模拟点击，dailyQuote 正确应用、点击后切换成功、无 JS 报错。

环境坑：`.vb_verify` 临时构建目录被 safe-delete 包装拦截（>50 文件触发批量确认且失败即中止），无法删除；但该目录已在 .gitignore，不影响提交与部署。

遗留：未提交（待用户确认后 commit）。

## 资源卡片点击直接跳转（去除右侧详情弹层摩擦）
- 现象：点击资源卡片会右侧弹出详情面板再给「访问原链接」控件，操作摩擦重；用户希望点击卡片（除编辑/删除控件）任意位置直接跳转。
- 改动 `assets/js/main.js` 的 grid 点击处理：卡片本身是 `<a target="_blank" href=link>`，当 `href` 为有效链接（非 `#`）时直接 `return` 放行原生跳转；仅当无原链接（`href === '#'`）才 `preventDefault()` + `openDetail()` 回退站内详情弹层。编辑/删除按钮逻辑不变（仍 `preventDefault` 后处理）。
- 验证：jsdom 加载 learning 页面真实产物模拟点击——有链接卡片 `detailOpen=false & defaultPrevented=false`（原生跳转、不弹面板）；无链接卡片 `detailOpen=true`（回退面板）。无 JS 报错。
- 注：未提交（待用户确认后 commit）。
