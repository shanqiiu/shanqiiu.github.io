# 2026-08-17

## 资源库「删除」功能补齐（CRUD 完整）
- 之前诊断：前端对 knowledge_items 仅实现 select/insert/update，缺 delete（DB 策略 knowledge_items_admin_delete 早已存在，前端未接）。
- 本次改动：
  - assets/js/main.js：动态卡片新增「删除」按钮（knowledge-delete-btn / data-delete-id），仅管理员态显示；grid 点击处理在编辑块后新增删除逻辑——window.confirm 确认后调 createSupabaseClient().from('knowledge_items').delete().eq('id', id)，成功则移除卡片。
  - assets/css/main.css：.knowledge-delete-btn 玫红警示样式（#f43f5e），与编辑按钮风格对齐，body.knowledge-admin-active 下显示。
- 至此资源库前端完整支持增删改查（C/U/R 此前已有，D 补齐）；静态卡片（Hugo markdown 生成）仍只能改源文件。
- 构建验证：hugo --noBuildLock 成功，JS/CSS 产物均含 knowledge-delete-btn。

## 环境坑（本轮）
- Read/Edit 工具参数无法传入（file_path/pattern 报 undefined），改用 Grep(ASCII pattern) 提取代码 + Python 内联 heredoc 做精确字符串替换。
- 网络错误重试导致 patch 脚本跑两遍，new 字符串含 old 致重复插入；修复：git checkout 还原 + patch 加幂等检查（已含 knowledge-delete-btn 则跳过）。

## 知识库方案 A：彻底放弃静态卡片，统一走 Supabase 动态卡片（15d25b8 之后）
- 用户确认采用「方案 A」：所有资源统一由 Supabase 动态卡片管理，放弃服务端静态卡片。
- 诊断澄清：所谓"静态卡片"实际来自 `data/knowledge_taxonomy.yaml` 的 `resources:` 段（非 content/learning markdown）；模板用 `{{ range $items }}`（list.html:90-112）服务端渲染，无编辑/删除按钮。分类树在 `top_levels`，表单下拉也用 `top_levels`。
- 改动：
  - layouts/learning/list.html：删除 `{{ range $items }}` 静态卡片循环，网格仅保留 `#resource-card-grid` + 空状态占位 `#resource-empty-state`（hidden，由 JS 控制）。
  - data/knowledge_taxonomy.yaml：`resources:` 置空为 `[]`（保留 top_levels/recommended_tags 分类树）。
  - assets/js/main.js：新增 `updateCategoryCounts()`（按实时卡片 data-path 前缀重算侧边栏各级分类 `<b>` 计数，含"全部"），`updateEmptyState()` 切换空状态；均在 `syncCards()` 末尾调用。`emptyEl` 引用已加。
  - assets/css/main.css：`.resource-empty-state` 空状态样式（虚线边框居中提示）。
- 验证：hugo --noBuildLock 构建 RC=0；产物 learning/index.html 不再含旧静态链接（vuejs.org/api），含 resource-empty-state；JS bundle 含 updateCategoryCounts / resource-card-dynamic / knowledge-delete-btn（增删改查完整保留）。
- 注意：原 13 条 resources 种子卡片不再静态展示，需用户通过「新增资源」表单重新入库（或在 Supabase 后台补数据）；本环境无 DB 写入权限，无法代迁。

## 种子资源补录清单（resources-seed-list.md）
- 方案 A 移除 resources 静态卡片后，把原 12 条种子数据从 `git show 15d25b8:data/knowledge_taxonomy.yaml` 取回，整理为 `resources-seed-list.md`（总览表 + 逐条字段），供登录后逐条补录到 Supabase。均为 external 外链、published 公开。
- 注意：实数为 12 条（非之前口误的 13 条）。

## 生成 Supabase 批量导入 SQL（supabase/seed-knowledge-items.sql）
- 读取 supabase/schema.sql 确认 knowledge_items 字段（title/description/content_markdown/link/category_1~3/tags text[]/item_type/status/created_by uuid 可空）。
- 生成 supabase/seed-knowledge-items.sql：12 条 insert，item_type 按一级分类语义映射（技术文章->article 8条、工具教程->tool 2条、其他图书->book 2条），content_markdown 留空，created_by 由 DB 自动。SQL Editor 以 postgres owner 执行可绕过 RLS 直接写入。
- 与 resources-seed-list.md 一并提交推送（供登录后补录或 SQL 批量导入二选一）。

## 首页「每日随机一句」升级（hero-quote）
- 需求：把原硬编码单句（白居易《简简吟》）升级为「每日随机一句」。
- 新增 data/quotes.yaml：16 条中文名句（text + source），纯数据、可随时编辑扩写，无需改代码。
- layouts/index.html：hero-quote 保留硬编码兜底句；其下新增 `<script type="application/json" id="hero-quotes-data">{{ hugo.Data.quotes | jsonify }}</script>`（改用 hugo.Data 替代已废弃的 .Site.Data，消掉弃用告警）。
- assets/js/main.js：新增 initHeroQuote()，按"年内第几天 % 数组长度"确定性选取——当天不变、每日轮换、遍历全部；填充 .hero-quote-text / .hero-quote-source。boot() 内调用。
- assets/css/main.css：.hero-quote-text 去掉 white-space:nowrap / word-break:keep-all（长句会被裁切），改 word-break:break-word + overflow-wrap:anywhere 允许换行。
- 验证：hugo --noBuildLock 构建 RC=0，JSON 数据已注入产物；.vb_verify 临时目录已清理。
- 注：未提交推送（用户仅说"升级"，未要求提交）。

## 首页每日一句新增「循环刷新」按钮
- 需求：在 hero-quote 旁加一个圆形 loop 图标按钮，单击随机切换一句（不刷新页面）。
- layouts/index.html：hero-quote 内新增 `<button class="hero-quote-refresh">`（内联循环箭头 SVG，因 svg-icon 集无 loop/refresh 图标）；aria-label/title="换一句"。
- assets/js/main.js：initHeroQuote() 重构——render(idx) 抽离；默认仍按年内第几天确定性选取；按钮 click 时随机取不同于当前的一句，加 .is-switching 触发文案淡出、200ms 后换文淡入，图标加 .is-spinning 旋转 0.5s 反馈。
- assets/css/main.css：.hero-quote-refresh 圆形按钮（pointer-events:auto 覆盖父级 none；毛玻璃底、--accent 主色、hover 填充+阴影、active 缩放）；.hero-quote-text/.source 加 opacity 过渡 + .is-switching 淡出；@keyframes hero-quote-spin 旋转；补 [data-theme="dark"] 暗色变体。
- 验证：hugo --noBuildLock 构建 RC=0，产物 HTML 含按钮、JS bundle 含 is-spinning/hero-quote-refresh。
- 注：本轮用户未要求提交推送。
