# 2026-08-10

## 首页 Bento 三处 UI 修复（assets/css/main.css）
- 标签卡片 `.tag-card` 改为圆角胶囊条：border-radius 999px + flex，内部 `.tag-cloud .tag` 改为无边框透明文字标签。
- 左侧时间线压缩行距以填充 176px 面板：`.timeline-card` padding 9px、`.timeline-list` gap 3px、title/date/desc 字号与行高收紧、dot top 5px（原因：面板 `overflow:hidden` + 固定行高导致截断）。
- `Navigation 导航` 标题与其左侧图标对齐 GitHub 热力图标题样式：color var(--text)、font-mono、font-size 0.95rem、letter-spacing 0；图标 16px 去 drop-shadow。
- 注意：此机未装 hugo，需运行 hugo server/build 重建 public 才能生效。

## 后续修正（同一天）
- 标签改法纠正：用户要的是**每个 .tag 是胶囊**，不是整块 .tag-card 变胶囊。已还原 .tag-card 为普通卡片（去掉 999px/flex），把胶囊样式下放到 `.tag-cloud .tag`：bg rgba(255,255,255,.07)+border+radius 999px+padding 3px 11px+font .76rem，并加 hover 高亮。
- 时间线"过宽"修正：`grid-template-columns` 由 `minmax(0,1fr) auto` 改为 `max-content auto`，`column-gap` 10px→8px，消除短标题(本科)与右侧日期间的大片空白。
- hugo 已于本机安装（v0.164.0 extended，%USERPROFILE%\bin，已写入用户 PATH 与 ~/.bashrc）。
- 构建坑：WorkBuddy 预览进程(picpreview.exe)锁定旧 public/*.xml，直接 hugo 报 Access denied；改用 `--destination .hugo_tmp` 构建后 mv 替换 public 绕过。

## 时间线排版二次优化（参考 Snipaste_2026-08-10_18-31-54.png）
- 问题：标题和日期仍挤在同一行，大小/层级不美观。
- 改动：
  - `layouts/index.html`：将 `.timeline-title` 与 `.timeline-desc` 包裹进 `.timeline-head`，日期 `.timeline-date` 移至下方。
  - `assets/css/main.css`：
    - `.timeline-head`：字号 0.88rem、行高 1.25、深色；`.timeline-title` 加粗 700；`.timeline-desc` 常规字重、次要色。
    - `.timeline-date`：0.7rem、var(--text-3)、margin-top 2px，单独一行。
    - `.timeline-list` gap 由 3px 改为 10px，与参考图的条目间距一致。
    - `.timeline-item` 改为 flex column，去掉 grid 布局。
- 效果：标题行 "本科 · 计算机科学与技术" 在上，日期 "2018-2022" 在下，与参考图结构一致。

## 复刻 template-web-main 首页面板/文字参数（assets/css/main.css #home-bento）
- 参考模板（Next.js, src/pages/index.tsx + globals.css）关键参数：卡片 padding 10px、radius 5px、卡片间 gap 10px、左列 250px、正文 14px、标签/次要 12px、区块标题 16px、时间线标题 13-14px/日期 11-12px。
- 已套用（仅 #home-bento 作用域，不动其他页）：
  - 新增 `#home-bento .info-card{padding:10px;border-radius:5px}`；`.nav-cards` radius 改 5px。
  - 文字：nav-cards-title 0.95→1rem；nav-card-title 0.95→.875rem(14px)；nav-card-desc 加 .75rem(12px)；github 标题 0.95→1rem；timeline-head .88→.875rem(14px)；timeline-date .7→.75rem(12px)；tag 字体 .76→.75rem、padding 3px11→4px10。
  - tag-card/timeline-card padding 统一 10px。
- 容器 max-width 980px / 左列 260px 已 ≥ 参考(768px/250px)，保留不缩窄（用户诉求是"偏小"，避免反而变小）。
- 时间线条目间距用户已定 gap:3px，未按参考 40px 连接线复刻（属独立诉求）。

## 添加右下角背景音乐播放器
- 文件：`layouts/partials/header.html`（播放器 DOM）、`assets/css/main.css`（样式）、`assets/js/main.js`（交互）、`static/audio/slam-dunk.wav`（占位音频）。
- 位置：固定于右下角 `page-nav-fab` 上方（right 18px / bottom 80px），移动端自动收窄。
- 特性：
  - 玻璃拟态卡片 + 樱木花道封面，与现有入口控件风格一致。
  - 播放/暂停（封面中央大按钮）、进度条拖拽、上一首/下一首（步进 10s）、静音切换。
  - 左上角小箭头可收起为右侧标签；再次点击标签展开；折叠状态写入 localStorage。
  - 默认循环播放，不自动播放（浏览器策略）。
- 音频占位：`slam-dunk.wav` 是 5 秒 440Hz 正弦波占位文件；需替换为真正的《好想大声说爱你》MP3 并修改 `src` 为 `/audio/slam-dunk.mp3`。
- 已重建 public/ 并验证 `music-player` DOM 与 CSS 均生效。

## 播放器修复（2026-08-11）
- Bug：折叠时整个 `.music-player`（含 `.music-tab`）被 translateX 移出屏幕，导致右侧标签也不可见、无法重新打开。
- 修复：折叠只把 `.music-body` 移出屏幕（`transform: translateX(100%+18px)`），`.music-tab` 保留在屏幕右缘作为入口；`.music-player.is-collapsed` 设 `pointer-events:none` 避免透明容器拦截底层点击，仅 tab 恢复 auto。
- 尺寸缩小：容器 width 280→220（移动端 260→200）；body padding 12→10、radius 14→12；标题 .82→.78rem、艺人 .7→.66、时间 .65→.6rem；封面 radius 10→8、margin 10→8；播放键 52→44、控制按钮 28→24、滑块手柄 12→11。
- 统一入口：移除独立的 `.music-tab`，将 `.music-toggle` 改为唯一控制按钮。展开时位于卡片左上角，显示左箭头（收起）；折叠时移动到屏幕右缘中央，显示音乐音符（展开）。同一元素动画过渡，消除割裂感。已重建验证，CSS 与 HTML 中均无 `music-tab` 残留。
