# 2026-08-19

## 主页 hero 区字体统一（用户选「全面统一」方案）
- 诊断：hero 区 3 处一致性问题 —— greeting 用等宽 `--font-mono`+`uppercase` 异类、info 行颜色不统一（职业行 accent 蓝 + 其余灰）、字号极差大（0.86→4.4rem）
- 改动：
  - `_base.css` `.hero-meta`：font-family `--font-mono`→`--font-sans`，去 `text-transform:uppercase`，letter-spacing 0.14em→0.04em
  - `_home.css` `.hero-info-wide`：color `--accent`→`--text-2`（信息行全部拉平为灰）
  - `_base.css` `.hero-name`：clamp(2.6,6vw,4.4rem)/900 → clamp(2.4,5vw,3.8rem)/800
  - `_base.css` `.hero-en`：clamp(1,2vw,1.4rem)/700/accent → clamp(1,2vw,1.3rem)/600/`--text-2`
- 验证：hugo 构建成功（397ms 无 error），产物 `main.min.*.css` 确认四类改动生效
- **环境坑（重要）**：本环境直接 `hugo` 命令被 PATH 解析成无 `.exe` 后缀入口，报 `Permission denied`；必须用绝对路径 `C:\Users\j00956244\bin\hugo.exe`（Git Bash 写法 `/c/Users/j00956244/bin/hugo.exe`）才能构建
- 状态：改动未 git commit，等用户确认「提交推送」

## 备注
- 全站 `--font-serif` 实为 `var(--font-sans)` 别名，即全站只有 sans + mono 两字族，无真衬线
- 主页 `.hero-quote`（每日一句）仍用 KaiTi 楷体，属刻意设计，本次未动

## 技术栈 Logo 化（用户从 yesicon 手动下载）
- yesicon.app 被 Cloudflare 拦截，用户手动下载 11 个 SVG 到 `C:\Users\j00956244\Desktop\icon`
- 复制到 `static/svg/`：git/python/pytorch/opencv/fastapi/docker/nginx/hugo/ollama/aiagent/qwen
- `params.toml` `hero.techStack` 8 项各加 `icon` 字段；新增 Ollama/AI Agent/Qwen 三项（icon=ollama/aiagent/qwen）
- 模板 `layouts/index.html` tech-logo 改 `<img class="tech-logo-icon" src="/svg/{{ . }}.svg">`，无 icon 降级 `<b>{{ .mark }}</b>`，删 `<em>` 名称
- `_home.css` `.tech-logo` grid→flex 居中、去 min-width、删 `.tech-logo em` 样式

## 技术栈去文字 + 导航压缩（用户两条指令）
- 删 tech-logo 文字标签（仅留图标），导航副文案 `white-space:nowrap`+省略号占一行，`nav-card` 删 `min-height:76px`

## 首页布局重构（重大，99749fb）
- 底部三栏：#home-bento 重写为 github-card（通栏）+ nav-cards（通栏 5 入口）+ home-bottom（now-card / gallery-card 占位斜纹 / music-embed）
- 导航面板 + GitHub 热图做成通栏（bento-full）
- 技术栈上移到 hero-content 底部（`.hero-tech` 卡片内含 tech-marquee/tech-track 双副本，`hover` 暂停）
- 删除左侧 `left-top-panels`/`tag-card`（标签云）
- 音乐播放器：header.html 包 `{{ if not .IsHome }}`；首页由 bento 内嵌 `#music-player`，`_components.css` 追加 `.music-embed .music-player{position:relative;...}` 去 fixed、直角卡片、隐藏 `.music-toggle`
- `params.toml` 新增 `[[hero.now]]`（title/desc/status: doing|plan|done）驱动「最近在做的事」
- `.gitignore` 加 `/.vb_learn_check/`；本地临时构建目录移 `/d/_hugo_stray_builds`

## Git 事故与修复（7d37ac2）
- 删笔记 md 时 `git add -A` 误删 `content/learning/_index.md` 且误收 `.vb_learn_build` 等大量构建产物
- 修复：从 b93eafc 恢复 `_index.md`、`git rm --cached` 移除误收、加 gitignore、物理目录移出仓库
- 教训：删文件用 `git rm <具体路径>` 而非 `git add -A`；`.vb_*` 本地构建目录务必 gitignore 并移出仓库

## 文章未显示排查
- content/post 5 篇均正常，线上/本地均可见；用户反馈「刷新后」正常 → 实为浏览器/CDN 缓存，非数据缺失

## 首页布局细节微调（参照 wuxianl 截图）
1. **hero 右上角入口**：GitHub/QQ 控件从 hero-content 内移出，放到 `.hero-inner` 右上角（`.hero-top-right` 绝对定位），与主题切换控件在同一视觉层级
2. **压缩 hero 高度**：`_base.css` 中 `.hero` padding 34px→18px、`.hero-inner` padding 16px 26px→12px 18px、grid gap 28px→18px、头像 176px→156px、姓名 3.8rem→3.2rem、info 行 0.88rem→0.82rem
3. **删除问候语**：移除 `params.toml` `hero.greeting` 与 `index.html` `.hero-meta` 行
4. **技术栈恢复文字 + 压窄**：`tech-logo` 内加 `.tech-name`，图标 28px→24px，`.hero-tech` padding 12px 14px→7px 10px，`.tech-track` gap 8px→5px
5. **GitHub 热图统计精简**：伪元素 `::before` 中英文两行删除，改由 JS 写入 `#github-stats` 单行中文（`X 次提交在 Y 年`），放在「GitHub 活跃度」标题后方；`.github-card-head` flex 布局
6. **导航栏压缩**：`.nav-cards` padding-top 56px→38px、gap 10px→8px；`.nav-card` padding 12px 14px→8px 10px、标题 0.875rem→0.82rem、图标区 34px→28px
7. **音乐播放器歌单**：`params.toml` 新增 `[[hero.playlist]]`；首页音乐 embed 加 `<ol class="music-playlist">`；`main.js` 扩展 `initMusicPlayer` 支持点击切歌、上一首/下一首切换、播放结束自动下一首；占位曲目目前复用同一音频文件，需后续替换真实音源
- 验证：hugo 构建成功（198ms）；期间遇到 hugo.exe 多进程残留导致 public 文件锁定，用 `taskkill /F /IM hugo.exe` 清理后恢复

## 红框问题澄清后五项修复（用户逐条澄清根因）
1. **头像文字环圆心对齐**：澄清「SIL 残字」非截断，而是 `.avatar-badge-text` 的 `transform-origin` 写错（桌面 78px/移动 68px），导致文字环绕 viewBox 中心 (88,88) 之外旋转、与头像不同心；改为 `88px 88px`，现在绕头像中心自旋
2. **社交入口移至页面右上角**：澄清应放在「整个页面右上角、夜间模式切换左侧」；从 `index.html` 删 `.hero-top-right`，改放入 `partials/header.html` 的 `.site-header` 内、`.theme-toggle` 之前；`_base.css` 给 `.site-header` 加 `display:flex` 与 `.header-socials` 样式（学习页仍用 `.hero-socials` 基类，未删）
3. **技术栈无缝循环**：澄清滚动条空白是循环接缝问题；将两份技术栈各包入 `.tech-group`（`gap:5px; padding-right:5px`），轨道去 `padding`，`translateX(-50%)` 精确等于一组宽度，接缝消除
4. **每日一句单击切换**：移除 `.hero-quote-refresh` 按钮；`main.js` 的 `initHeroQuote` 改为绑定 `.hero-quote` 整块点击换句，加 `.is-clickable`（`pointer-events:auto; cursor:pointer`）；CSS 删 refresh 样式、加 hover 描色
5. **GitHub 活跃度留白压缩**：`#github-heatmap` `padding-top` 30px→8px；并把年份切换控件 `.hm-year-controls` 从热力图内（absolute）移到 `.github-card-head` 标题行（JS `renderHeatmap` 中 `head.appendChild(ctrl)`），CSS 改 `position:static`，彻底去掉标题下方空白带
- 验证：`rm -rf public` 后 hugo 构建成功（267ms，88 pages）；确认 header-socials=1/hero-top-right=0/tech-group=2/hero-quote-refresh=0/transform-origin 88px=2
- **环境坑复现**：构建再遇 `public/tags/层级分类/page/1/index.html` Access is denied（预览服务器锁文件），`rm -rf public` 重建解决

## GitHub 热力图统计文案位置修正
- 问题：`.github-card-head` 用 `justify-content:space-between` + 三个 flex 子项（title / github-stats / hm-year-controls），导致统计文案被推到中间、不协调
- 修复：head 改 `justify-content:flex-start` + `gap:10px`（title 与 stats 左对齐相邻）；`.hm-year-controls` 加 `margin-left:auto` 钉到右侧；统计文案现位于「活跃度」右侧、面板左侧
- 验证：`rm -rf public` 后 hugo 构建成功（293ms），minified CSS 已含新规则

## 首页视口锁定与上下等距布局
- 需求：底部三模块（最近在做的事/图库/音乐）不超出页面底部；整面板距页面顶部/底部间距一致
- 根因：`.main` 上下 padding 不等（24/52）+ `#home-bento` 与 `.container` 各带 18px 底 padding（底部合计多 ~88px）；内容超高直接溢出视口
- 改动（_home.css / _components.css）：
  - `body:has(#home-bento) .main`：`height:100dvh; padding:26px 0`（等距）；`overflow:hidden`；去 `justify-content:safe center`
  - flex 填充链：`#home-bento`→`.container`→`.bento-stack`→`.home-bottom` 全部 `flex:1 1 auto; min-height:0`；hero `flex:0 0 auto`
  - 底部三卡 `min-height:0; overflow:hidden`；内部滚动：`.now-list`/`.gallery-grid`/`.music-playlist` 均 `overflow-y:auto`（thin scrollbar）
  - 音乐：`.music-player` 改 flex 列填充，`.music-body` `flex:0 0 auto`
  - 移动端（≤900px）回退：解除视口锁定恢复自然滚动、overflow 全部 visible
- 页脚为 `position:fixed; bottom:4px` 悬浮统计条（不在文档流），26px 底距可避开
- **环境坑**：本轮 `rm -rf public` 被安全删除包装器拦截（trash failed → fail-closed）；直接 hugo 覆盖构建即可，残留未引用的旧 hash CSS 无害（CI 干净构建）

## 视觉协调微调（底部留白/热力图截断/技术栈方框/hero 抬升）
- 底部三卡：卡内 gap 10→4px，`.info-title` scoped `margin-bottom:0`，`.music-playlist` margin-top 8→4px
- GitHub 热力图截断根因：github-card/nav-cards 在 flex 链中默认可压缩，超高时被 overflow:hidden 裁掉底部 → 二者设 `flex:0 0 auto`，压缩全部由 home-bottom（内部滚动）承担，热力图恢复完整
- `.tech-logo` 去小方框（background transparent / border 0 / padding 缩为 2px 5px）
- hero 底部抬升：`.hero-tech` margin-top 10→6px、padding 7→6px；`body:has(#home-bento) .hero-inner` padding-bottom 12→8px
- 验证：hugo 构建成功（412ms），minified CSS 含全部新规则

## 每日一句居中参考系调整
- 需求：每日一句原以整个 hero-content（含技术栈区）高度 50% 居中；改为以「面板顶部 ~ 技术栈顶部」区间居中
- 实现：index.html 将 hero-title/hero-info-grid/hero-quote/quotes-data 包进新容器 `.hero-main`（hero-tech 留在其外）；_home.css 加 `.hero-main{position:relative;...}`，绝对定位的 `.hero-quote(top:50%)` 参考系随之变为 hero-main
- 环境坑：public 再遇 Access is denied（预览进程锁文件），本次解法：`--destination D:/_hugo_stray_builds/home-check` 临时目录验证 + 稍后重试 public 构建成功；注意 Windows hugo.exe 不认 Git Bash 的 /d/ 路径，须用 D:/ 风格

## 底部三面板风格优化（参考 wuxianl 布局）
- 参考图：Snipaste_2026-08-19_16-41-05.png（扁平、简洁、薄边）
- 底部三卡去玻璃化：`background: transparent; border: 1px solid var(--border); box-shadow: none; padding: 10px 12px`（覆盖 _home.css 第 1042 行 #home-bento .info-card 厚重渐变）
- 事项列表：圆点改为等宽编号（`now-num`），右侧加状态文字（进行中/计划中/已完成），`now-status-doing` 走 accent 色、`done` 走绿
- 图库卡头：新增 `.gallery-head` flex 容器，标题左侧、`< 1/4 >` 计数器翻页右侧，箭头悬停高亮
- 音乐面板内部：`.music-body` 背景/边框/圆角/内边距全部清零，与外层扁平卡风格一致
- 验证：临时目录构建成功（245ms），随后 public 重建成功（386ms）；HTML 含 now-num/now-status(进行中×3,计划中×1)/gallery-head(1)/gallery-counter(1)/gallery-arrow(2)

## 底部三面板风格优化（参考 wuxianl 布局）
- 参考图：Snipaste_2026-08-19_16-41-05.png（扁平、简洁、薄边）
- 底部三卡去玻璃化：`background: transparent; border: 1px solid var(--border); box-shadow: none; padding: 10px 12px`（覆盖 _home.css 第 1042 行 #home-bento .info-card 厚重渐变）
- 事项列表：圆点改为等宽编号（`now-num`），右侧加状态文字（进行中/计划中/已完成），`now-status-doing` 走 accent 色、`done` 走绿
- 图库卡头：新增 `.gallery-head` flex 容器，标题左侧、`< 1/4 >` 计数器翻页右侧，箭头悬停高亮
- 音乐面板内部：`.music-body` 背景/边框/圆角/内边距全部清零，与外层扁平卡风格一致
- 验证：临时目录构建成功（245ms），随后 public 重建成功（386ms）；HTML 含 now-num/now-status(进行中×3,计划中×1)/gallery-head(1)/gallery-counter(1)/gallery-arrow(2)
